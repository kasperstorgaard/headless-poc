section.layout-hero(style={
    'background-image': 'url('+ section.backgroundImage.value[0].url +')'
  })
  h1.layout-hero-headline #{section.headline.value}
  p #{section.body.value}
  a.sif-button #{section.ctaText.value}